fix(programs): make program navigation compact and minimal

- replace the oversized program switcher styling with a compact segmented navigation
- keep only the three program names with no helper text, icons, arrows or subtitles
- use a subtle active state instead of large cards
- keep navigation small on desktop and mobile
- use a new stylesheet filename to avoid stale GitHub Pages/browser cache
- preserve aria-current and one-click switching between program pages
